// generic settings
log on
hostname "{{it.hostname}}"
rcon_password "{{it.rcon_password}}"
sv_logflush 1
sv_pausable 1

// team settings
mp_teamname_1 "{{it.teamname_ct}}"
mp_teamname_2 "{{it.teamname_t}}"
// @TODO: FIGURE THIS OUT
// mp_teamflag_1 "{{it.teamflag_ct}}"
// mp_teamflag_2 "{{it.teamflag_t}}"
// {{@if(it.shortname_ct)}}
// mp_teamlogo_1 "{{it.shortname_ct}}"
// {{/if}}
// {{@if(it.shortname_t)}}
// mp_teamlogo_2 "{{it.shortname_t}}"
// {{/if}}

// competition settings
mp_teammatchstat_holdtime 7200
{{@if(it.match_stat)}}
mp_teammatchstat_txt "{{it.match_stat}}"
{{/if}}
{{@if(it.stat_ct)}}
mp_teammatchstat_1 "{{it.stat_ct}}"
{{/if}}
{{@if(it.stat_t)}}
mp_teammatchstat_2 "{{it.stat_t}}"
{{/if}}

// bot configuration
bot_allow_rogues 0
bot_chatter minimal
bot_defer_to_human_goals 0
bot_defer_to_human_items 0
bot_join_after_player 0
bot_quota 0
bot_quota_mode "normal"
sv_auto_adjust_bot_difficulty 0
sv_damage_print_enable "{{it.damage_prints}}"

// server settings
mp_autokick 0
mp_autoteambalance 0
mp_c4timer 40
mp_defuser_allocation 0
mp_disable_autokick
mp_endmatch_votenextmap 0
mp_forcecamera 3
mp_free_armor 0
mp_freezetime 8
mp_friendlyfire 1
mp_halftime 1
mp_halftime_duration 10
mp_limitteams 0
mp_logdetail 3
mp_match_can_clinch 1
mp_match_end_restart 1
mp_match_restart_delay 120
mp_maxmoney 16000
mp_maxrounds "{{it.maxrounds}}"
mp_overtime_enable "{{it.ot}}"
mp_overtime_maxrounds 6
mp_roundtime 1.92
mp_solid_teammates 1
mp_startmoney 800
mp_timelimit 0
mp_tkpunish 0
mp_weapons_allow_zeus 1

// FACEIT-specific: dynamic starting side for the human
// (fed via serverCfgData.humanteam = "T" | "CT" | "any")
mp_humanteam "{{it.humanteam}}"

// auto-assign player to team unless spectating mode
{{@if(it.spectating)}}
mp_force_pick_time 15
{{#else}}
mp_force_pick_time 0
{{/if}}

// plugin: liga
liga_gameover_delay "{{it.gameover_delay}}"
liga_spectating "{{it.spectating}}"
isAWP "{{it.isAWP}}"

// demo overrides
{{@if(it.demo)}}
sv_cheats 0
{{/if}}



